services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: doan-ltw-web
    ports:
      - "8080:8080"
    environment:
      ASPNETCORE_ENVIRONMENT: Development
      ASPNETCORE_URLS: http://+:8080
